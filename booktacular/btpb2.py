
mappings = {
    # "/build/acTotal/acTotal": "armor_class_",  # ['build']['acTotal']['acTotal']
    # "/build/name": "character_name_", # ['build']['name']
}

# Load from mappings near json file instead (generated, ready for user to edit)
# TODO: save default one there